# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Virtual env
.venv/
.venv-*/
venv/

# IDE
.vscode/
.idea/

# Story Lifecycle local data (user-level, not in repo)
.story/
story.db
checkpoint.db
.story-context/
.story-knowledge/
.story-done/
.story-runs/

# Runtime workspace created when running story commands from the repo root
ws/

# OS
.DS_Store
Thumbs.db

# Stray output files
Done
Installing
Stopping
To
.worktrees/
story/

# Playwright MCP artifacts
.playwright-mcp/
context-tab.png

# 运行日志 / 崩溃产物
.story-serve.*.log
*.stackdump

# Real-E2E calculator scenario: AI 生成的产物（每次跑前 reset 回 red）
packages/testing/src/testing/scenarios/calculator/calculator.py

# AI tool runtime artifacts (brainstorm sessions, plans — process not product)
.superpowers/
.qoder/
.hermes/
.workbuddy/
.codex/
.kimi-code/
.claude/

# eval 层产物（gold 抽取 / 评分 / 回放）
packages/eval/dataset/
packages/eval/results/
.tmp/eval_runs/

# round2 释放缓存（story_home/ws/gold 全排除，只留 profiles 模板）
packages/eval/sandbox/story_home/
packages/eval/sandbox/ws/
packages/eval/sandbox/gold/
packages/eval/sandbox/selfcheck_report.json

# 测试覆盖率 / 运行日志 / ZCode 工作区（runtime artifact，过程非产物）
.coverage
*.log
.zcode/

# 本地 token 用量报告（每次跑生成，不入库）
docs/local-token-usage-chart.html
docs/local-token-usage-report.md
