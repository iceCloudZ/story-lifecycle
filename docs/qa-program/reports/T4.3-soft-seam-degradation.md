# T4.3 · ② 上下文:SOFT 缝降级

> 任务卡:[tasks.md#阶段四--补齐其余模块](../tasks.md)
> 日期:2026-07-08 · 窗口:#当前
> 状态:[已完成]

## 做了什么(What)

为 SOFT 缝(架构不变量 #4)补了降级测试,验证 miner/knowledge 包不可 import 时 lifecycle 不崩:
- 新建 `test_soft_seam_degradation.py`,覆盖:
  - `miner.config` 不可 import → `get_transcript_context` 返回 `None`。
  - 显式配置了 `context_provider` 但对应模块不可 import → 仍返回 `None`。
  - `knowledge` 包不可 import → `get_knowledge_context` 不崩,返回内容中不含「知识库」段落(或整体为 None)。
  - 无 `task_type` 时 `get_knowledge_context` 直接返回 `None`,不触发 knowledge import。
- 未修改生产代码;`context_providers/__init__.py` 和 `knowledge_provider.py` 已有 try/except 降级逻辑,测试只是锁定行为。

## 代码改动(Changes)

| 文件 | 改动 | 说明 |
|---|---|---|
| `packages/story-lifecycle/tests/test_soft_seam_degradation.py` | 新建 | 4 个 SOFT 缝降级测试 |

commit: `b2ebf1a8`

## 验收结果(Acceptance — 必须实际跑过)

**命令**:
```bash
./.venv-monorepo-test/Scripts/python.exe -m pytest \
  packages/story-lifecycle/tests/test_soft_seam_degradation.py -v
```

**输出**:
```text
============================= test session starts =============================
platform win32 -- Python 3.12.10, pytest-9.1.1, pluggy-1.0.0 -- D:\github\story-lifecycle\.venv-monorepo-test\Scripts\python.exe
cachedir: .pytest_cache
rootdir: D:\github\story-lifecycle\packages\story-lifecycle
configfile: pyproject.toml
plugins: anyio-4.14.1, asyncio-1.4.0
asyncio: mode=Mode.STRICT, debug=False, asyncio_default_loop_scope=None, asyncio_default_test_loop_scope=function
collecting ... collected 4 items

packages\story-lifecycle\tests\test_soft_seam_degradation.py::TestTranscriptContextSoftSeam::test_returns_none_when_miner_unavailable PASSED [ 25%]
packages\story-lifecycle\tests\test_soft_seam_degradation.py::TestTranscriptContextSoftSeam::test_returns_none_when_configured_provider_unavailable PASSED [ 50%]
packages\story-lifecycle\tests\test_soft_seam_degradation.py::TestKnowledgeContextSoftSeam::test_returns_something_without_knowledge_package PASSED [ 75%]
packages\story-lifecycle\tests\test_soft_seam_degradation.py::TestKnowledgeContextSoftSeam::test_returns_none_on_unknown_task_type PASSED [100%]

============================== 4 passed in 0.27s ==============================
```

**结论**:✅ 全绿通过

## 发现(可选,Findings)

- `get_transcript_context` 在两条路径都做了降级:
  - 无显式配置时靠 `_default_provider_cfg()` 的 try/except。
  - 有显式配置时靠 `_load_provider` + 外层 try/except。
- `knowledge_provider.py` 的降级点在 `_build_knowledge_index_section` 的 `from knowledge import KnowledgeIndex`;ImportError 被捕获后返回空字符串,不影响 bootstrap/phase2/graph 等段落。
- `monkeypatch.setitem(sys.modules, "...", None)` 是让 import 抛 ImportError 的最轻量方式,且不破坏真实包(测试结束后自动恢复)。

## 交接 NOTE(下一窗口接什么)

- **本卡是否完成**:完成
- **下一窗口接什么**:阶段四 T4.4 · ⑤ 交付:worktree 清理无残留。读 `packages/story-lifecycle/src/story_lifecycle/orchestrator/workspace/worktree/` 下的 cleanup 路径,验证 prepare → cleanup 后目录删除、关联 PTY 关闭。
- **踩过的坑**:
  - `knowledge_provider.get_context()` 在 knowledge 包缺失时仍可能返回非 None(只要 task_type 能解析且 base_path 下有文件);任务卡说的「优雅跳过」指不崩,不是一定返回 None。
- **保留的上下文**:
  - 被测文件 1:`packages/story-lifecycle/src/story_lifecycle/knowledge/context_providers/__init__.py`
  - 被测文件 2:`packages/story-lifecycle/src/story_lifecycle/knowledge/context_providers/knowledge_provider.py`
  - 新测试:`packages/story-lifecycle/tests/test_soft_seam_degradation.py`
