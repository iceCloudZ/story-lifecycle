# T3.1 · clarify MCP 阻塞 + 交互式双路径

> 任务卡:[tasks.md#阶段三--hitl模块](../tasks.md)
> 日期:2026-07-08 · 窗口:#当前
> 状态:[已完成]

## 做了什么(What)

为 design 阶段「clarify 问人」的双路径补了契约/回归测试:
- MCP 阻塞路径:`handle_clarify_call` 落 clarification_request 事件 + fake poll 返回 answer → 验证 MCP result 含人答。
- MCP 超时路径:fake poll 返回 None → 验证返回 conservative fallback 文案且 `isError=False`。
- 交互式终端路径:`build_design_dimensions_section(interactive=True)` 验证文案含「在终端直接问人」、不含 `mcp__lifecycle__clarify`。
- 默认 headless 路径验证仍引导调用 `mcp__lifecycle__clarify`。
- 验证该 section 仅在 `stage="design"` 注入。

## 代码改动(Changes)

| 文件 | 改动 | 说明 |
|---|---|---|
| `packages/story-lifecycle/tests/test_clarify_dual_path.py` | 新建 | 覆盖 MCP 阻塞/超时 + 交互式双路径 + stage 门控 |

commit: `942ff9d8`

## 验收结果(Acceptance — 必须实际跑过)

**命令**:
```bash
./.venv-monorepo-test/Scripts/python.exe -m pytest \
  packages/story-lifecycle/tests/test_clarify_dual_path.py -v
```

**输出**:
```text
============================= test session starts =============================
platform win32 -- Python 3.12.10, pytest-9.1.1, pluggy-1.0.0 -- D:\github\story-lifecycle\.venv-monorepo-test\Scripts\python.exe
cachedir: .pytest_cache
rootdir: D:/github/story-lifecycle/packages/story-lifecycle
configfile: pyproject.toml
plugins: anyio-4.14.1, asyncio-1.4.0
asyncio: mode=Mode.STRICT, debug=False, asyncio_default_loop_scope=None, asyncio_default_test_loop_scope=function
collecting ... collected 5 items

packages\story-lifecycle\tests\test_clarify_dual_path.py::TestClarifyMcpBlockingPath::test_handle_clarify_call_logs_request_and_returns_answer PASSED [ 20%]
packages\story-lifecycle\tests\test_clarify_dual_path.py::TestClarifyMcpBlockingPath::test_handle_clarify_call_timeout_returns_conservative_fallback PASSED [ 40%]
packages\story-lifecycle\tests\test_clarify_dual_path.py::TestClarifyInteractiveTerminalPath::test_interactive_branch_asks_human_in_terminal PASSED [ 60%]
packages\story-lifecycle\tests\test_clarify_dual_path.py::TestClarifyInteractiveTerminalPath::test_non_interactive_branch_uses_mcp_tool PASSED [ 80%]
packages\story-lifecycle\tests\test_clarify_dual_path.py::TestClarifyInteractiveTerminalPath::test_non_design_stage_returns_empty PASSED [100%]

============================== 5 passed in 0.29s ==============================
```

**结论**:✅ 全绿通过

## 发现(可选,Findings)

- `prompt_sections.py` 末尾 `__all__` 未列出 `build_design_dimensions_section`,但函数仍可导入;对测试无影响,只是公开 API 列表不完整。建议后续顺手补到 `__all__`。
- 现有 `test_clarify_mcp.py` 已覆盖 `poll_clarify_answer` 与 `get_pending_clarification`,本卡聚焦「双路径对比」,未重复深入 poll 细节。

## 交接 NOTE(下一窗口接什么)

- **本卡是否完成**:完成
- **下一窗口接什么**:阶段三 T3.2 · supervisor HITL 决策。读 `packages/story-lifecycle/src/story_lifecycle/orchestrator/infra/terminal/supervisor.py` 的 `decide_response`(27) 与 `handle_pty_output`(84),补 mock-LLM 决策边界测试。
- **踩过的坑**:无
- **保留的上下文**:
  - MCP 路径核心:`story_lifecycle.orchestrator.mcp.clarify_server.handle_clarify_call`
  - 交互式路径核心:`story_lifecycle.orchestrator.engine.prompt_sections.build_design_dimensions_section`
  - fallback 文案关键词:`conservative` / `最保守` / `自行`
