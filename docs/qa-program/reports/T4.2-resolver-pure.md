# T4.2 · ② 上下文:resolver 零副作用不变量

> 任务卡:[tasks.md#阶段四--补齐其余模块](../tasks.md)
> 日期:2026-07-08 · 窗口:#当前
> 状态:[已完成]

## 做了什么(What)

为架构不变量 #1(ContextResolver 只读、零副作用)新增守护测试:
- 新建 `test_resolver_pure.py`,覆盖:
  - `resolve` 调用前后 story DB 记录完全一致(含多次调用)。
  - `.story/` 工作区目录下文件 mtime 不变、无新增文件。
  - 多次 `resolve` 返回的 `ContextBundle` 关键字段一致(idempotent read)。
- 未修改生产代码;`ContextResolver` 本身已实现为纯读取,测试只是把它锁定下来。

## 代码改动(Changes)

| 文件 | 改动 | 说明 |
|---|---|---|
| `packages/story-lifecycle/tests/test_resolver_pure.py` | 新建 | 3 个零副作用不变量测试 |

commit: `474912ae`

## 验收结果(Acceptance — 必须实际跑过)

**命令**:
```bash
./.venv-monorepo-test/Scripts/python.exe -m pytest \
  packages/story-lifecycle/tests/test_resolver_pure.py -v
```

**输出**:
```text
============================= test session starts =============================
platform win32 -- Python 3.12.10, pytest-9.1.1, pluggy-1.0.0 -- D:\github\story-lifecycle\.venv-monorepo-test\Scripts\python.exe
cachedir: .pytest_cache
rootdir: D:\github\story-lifecycle\packages\story-lifecycle
configfile: pyproject.toml
plugins: anyio-4.14.1, asyncio-1.4.0
asyncio: mode=Mode.STRICT, debug=False, asyncio_default_loop_scope=None, asyncio_default_test_loop_scope=function
collecting ... collected 3 items

packages\story-lifecycle\tests\test_resolver_pure.py::TestContextResolverPure::test_resolve_does_not_modify_story_db PASSED [ 33%]
packages\story-lifecycle\tests\test_resolver_pure.py::TestContextResolverPure::test_resolve_does_not_touch_workspace_files PASSED [ 66%]
packages\story-lifecycle\tests\test_resolver_pure.py::TestContextResolverPure::test_resolve_returns_consistent_bundle PASSED [100%]

============================== 3 passed in 0.37s ==============================
```

**结论**:✅ 全绿通过

## 发现(可选,Findings)

- `ContextResolver.resolve` 确实只读:它调用 `db.get_story` / `db.get_story_projects` / `db.get_project` 等查询函数,并直接执行 `SELECT` SQL,没有写操作。
- 文件系统快照通过 mtime_ns 比对,能发现任何意外写盘;目前通过。
- 该测试未来可移入 `tests/invariants/`(T5.2 规划),目前放在 `tests/` 根目录与任务卡一致。

## 交接 NOTE(下一窗口接什么)

- **本卡是否完成**:完成
- **下一窗口接什么**:阶段四 T4.3 · ② 上下文:SOFT 缝降级。读 `packages/story-lifecycle/src/story_lifecycle/knowledge/context_providers/__init__.py` 的 `get_transcript_context` 与 `knowledge_provider.py`,用 monkeypatch 模拟 `import miner` / `import knowledge` 抛 ImportError,断言 lifecycle 不崩。
- **踩过的坑**:
  - `resolver.py` 实际路径在 `orchestrator/context/`,不是任务卡字面路径。
  - `_get_story_documents` 等辅助函数直接执行 SQL,测试不需要真实数据,`upsert_story` 即可。
- **保留的上下文**:
  - 被测文件:`packages/story-lifecycle/src/story_lifecycle/orchestrator/context/resolver.py`
  - 核心类/方法:`ContextResolver.resolve(story_key)`
  - 新测试:`packages/story-lifecycle/tests/test_resolver_pure.py`
