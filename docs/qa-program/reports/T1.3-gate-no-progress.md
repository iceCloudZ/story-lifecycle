# T1.3 · no_progress 终止(防死循环)

> 任务卡:[tasks.md#T1.3](../tasks.md)
> 日期:2026-07-08 · 窗口:#1
> 状态:[阻塞]

## 做了什么(What)

按 T1.3 任务卡要求,对 `orchestrator/evaluation/` 目录做了 `no_progress` / `no-progress` / `consecutive`
全文搜索,确认是否存在 no_progress 终止逻辑。

搜索结果:
- `orchestrator/evaluation/evaluator_loop.py:4` 仅在一行注释中提到:
  > "detect_no_progress, _get_stage_config_from_state) was LangGraph-era code; ISS-008 removed these dead parts."
- `gate.py` 中无任何 `no_progress` / `consecutive` 相关逻辑。
- 整个 `orchestrator/evaluation/` 目录无 active 的 no_progress 检测实现。

但 `packages/story-lifecycle/README.md:93` 仍列出:
> `- no_progress：连续无改善，自动终止等人工`

因此现状是 **README 宣称存在,但代码未实现**。

## 代码改动(Changes)

无新增测试文件(因逻辑不存在,无法写验收测试)。

commit: _(待提交后补)_

## 验收结果(Acceptance — 必须实际跑过)

**命令**:
```bash
./.venv-monorepo-test/Scripts/python.exe -m pytest packages/story-lifecycle/tests/test_gate_no_progress.py -v
```

**输出**:
```
ERROR: file or directory not found: packages/story-lifecycle/tests/test_gate_no_progress.py
```

(测试文件未创建,因为 `orchestrator/evaluation/` 下没有 no_progress 逻辑可测。)

**结论**:❌ 无验收可跑。按任务卡要求,此情况标 `[阻塞]`。

## 发现(Findings)

- **[文档/实现不符]** `README.md` 将 `no_progress: 连续无改善，自动终止` 列为系统能力,
  但 `orchestrator/evaluation/` 中对应的 `detect_no_progress` 等代码已被 ISS-008 移除。
  - 影响:用户/开发者会误以为 gate 有 no_progress 收敛保护,实际不存在。
  - 建议后续:
    1. 若 FC 模式下 no_progress 确实由 LLM/planner 内化,应在 README 中明确说明,
       并删除/改写「自动终止」等暗示系统级检测的文案。
    2. 若仍需系统级 no_progress 检测,应在 `gate.py` 或 `evaluator_loop.py` 中补实现,
       并配回归测试。

- **[相关但非 gate 的 no_progress 引用]** `orchestrator/engine/shadow_router.py` 和
  `orchestrator/observability/debug_packet.py` 也提到 `no_progress`,但它们分别属于
  provider 重试路由和调试/诊断工具,不是 verify gate 的 no_progress 终止逻辑,
  不满足 T1.3 的代码落点要求。

## 交接 NOTE(下一窗口接什么)

- **本卡是否完成**:未完成。需要 owner 决定是「文档修正」还是「补实现」。
- **下一窗口接什么**:owner 二选一:
  - **选项 A(文档修正)**:修改 `packages/story-lifecycle/README.md`,删除或澄清 `no_progress`
    相关描述(说明它是 FC/LLM 内化,非系统检测),然后 T1.3 转 `[已完成]`(已澄清)。
  - **选项 B(补实现)**:在 `orchestrator/evaluation/gate.py` 或 `evaluator_loop.py` 中实现
    no_progress 检测(如连续 N 轮相同 HIGH finding 则 fail),写 `test_gate_no_progress.py`,
    验收全绿后 T1.3 转 `[已完成]`。
- **踩过的坑**:
  - `grep` 范围要限定在 `orchestrator/evaluation/`,其他模块的 `no_progress` 引用容易混淆。
  - `evaluator_loop.py` 顶部注释明确说 `detect_no_progress` 是死代码且已被移除,这是关键证据。
- **保留的上下文**:
  - README 宣称点:`packages/story-lifecycle/README.md:93`
  - 代码证据:`packages/story-lifecycle/src/story_lifecycle/orchestrator/evaluation/evaluator_loop.py:4`
  - 无 active 实现:`orchestrator/evaluation/gate.py` 无 no_progress 相关代码
