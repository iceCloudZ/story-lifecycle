# T2.1 · FC 规划循环(mock LLM 生成 actions)

> 任务卡:[tasks.md#T2.1](../tasks.md)
> 日期:2026-07-08 · 窗口:#1
> 状态:[已完成]

## 做了什么(What)

新建 `packages/story-lifecycle/tests/test_fc_planning_loop.py`,用 mock LLM 驱动
`planner.py:172 run_orchestrator_agent` 的完整 FC 规划循环:

1. mock `get_llm()` 返回的 `invoke_with_tools`:第一轮返回预设的 `plan_step` ×2 + `skip_stage` ×1,
   第二轮返回空 tool_calls(规划结束)。
2. 调用 `run_orchestrator_agent(...)`。
3. 断言返回的 `actions` 包含 2 个 launch action(design/build)和 1 个 skip action(verify)。
4. 断言 story DB 的 `context_json` 中:
   - `_agent_actions` 与返回的 actions 一致
   - `_plan_confirmed == False`(暂停语义)
   - `plan_summary` 非空且含 stage 信息

未调用真实 LLM。

## 代码改动(Changes)

| 文件 | 改动 | 说明 |
|---|---|---|
| `packages/story-lifecycle/tests/test_fc_planning_loop.py` | 新建 | T2.1 FC 规划循环 mock LLM 测试 |
| `docs/qa-program/README.md` | 修改 | T2.1 状态更新为 `[已完成]` |

commit: _(待提交后补)_

## 验收结果(Acceptance — 必须实际跑过)

**命令**:
```bash
./.venv-monorepo-test/Scripts/python.exe -m pytest packages/story-lifecycle/tests/test_fc_planning_loop.py -v
```

**输出**:
```
============================= test session starts =============================
platform win32 -- Python 3.12.10, pytest-9.1.1, pluggy-1.6.0 -- D:\github\story-lifecycle\.venv-monorepo-test\Scripts\python.exe
cachedir: .pytest_cache
rootdir: D:\github\story-lifecycle\packages\story-lifecycle
configfile: pyproject.toml
plugins: anyio-4.14.1, asyncio-1.4.0
asyncio: mode=Mode.STRICT, debug=False, asyncio_default_default_loop_scope=None, asyncio_default_test_loop_scope=function
collecting ... collected 1 item

packages\story-lifecycle\tests\test_fc_planning_loop.py::test_fc_planning_loop_generates_actions_and_pauses PASSED [100%]

============================== 1 passed in 0.14s ==============================
```

**结论**:✅ 全绿通过。

## 发现(Findings)

无。`run_orchestrator_agent` 在 mock LLM 下按预期生成 actions 并进入暂停状态。

## 交接 NOTE(下一窗口接什么)

- **本卡是否完成**:完成。
- **下一窗口接什么**:继续 T2.2 · plan_confirm 暂停语义。
  被测函数:`orchestrator/engine/planner.py:468 continue_orchestrator_agent`。
  目标:证明 `_plan_confirmed=False` 时 actions 未执行,confirm 后(`_plan_confirmed=True`)才执行。
  需 mock adapter 的 launch 方法,不真起 CLI。
- **踩过的坑**:
  - `invoke_with_tools` 会被多次调用(每轮一次),mock 要用序列化的 side_effect 或队列,
    第一轮返回 tool_calls,第二轮返回空列表才能让循环正常结束。
  - `run_orchestrator_agent` 使用 `with_story_key()` 装饰器,会重入时通过 story_key 查找 story,
    所以测试里必须先 `db.create_story`。
- **保留的上下文**:
  - 测试文件:`packages/story-lifecycle/tests/test_fc_planning_loop.py`
  - 被测函数:`story_lifecycle/orchestrator/engine/planner.py:172 run_orchestrator_agent`
  - 关键写盘逻辑:`planner.py:333-334` 写 `_agent_actions` 和 `_plan_confirmed`
