# T1.4 · evaluator_loop repair-packet 构造

> 任务卡:[tasks.md#T1.4](../tasks.md)
> 日期:2026-07-08 · 窗口:#1
> 状态:[已完成]

## 做了什么(What)

新建 `packages/story-lifecycle/tests/test_repair_packet.py`,对 `evaluator_loop.py:29 build_repair_packet`
做输入→输出契约测试:

1. 返回的 packet 字符串包含 `stage` 名和 `round_num`。
2. 包含每条 finding 的 `description`。
3. 包含 `plan_summary` 和 verification status。
4. 空 `findings` list 不抛异常,仍能产出 packet。
5. `write_file=True` 时,按预期路径 `.story/context/<key>/repair_<stage>_round<N>.md` 写入文件。

所有断言采用子串包含,不锁死 packet 格式。

## 代码改动(Changes)

| 文件 | 改动 | 说明 |
|---|---|---|
| `packages/story-lifecycle/tests/test_repair_packet.py` | 新建 | T1.4 repair-packet 契约测试 |
| `docs/qa-program/README.md` | 修改 | T1.4 状态更新为 `[已完成]` |

commit: _(待提交后补)_

## 验收结果(Acceptance — 必须实际跑过)

**命令**:
```bash
./.venv-monorepo-test/Scripts/python.exe -m pytest packages/story-lifecycle/tests/test_repair_packet.py -v
```

**输出**:
```
============================= test session starts =============================
platform win32 -- Python 3.12.10, pytest-9.1.1, pluggy-1.6.0 -- D:\github\story-lifecycle\.venv-monorepo-test\Scripts\python.exe
cachedir: .pytest_cache
rootdir: D:\github\story-lifecycle\packages\story-lifecycle
configfile: pyproject.toml
plugins: anyio-4.14.1, asyncio-1.4.0
asyncio: mode=Mode.STRICT, debug=False, asyncio_default_default_loop_scope=None, asyncio_default_test_loop_scope=function
collecting ... collected 5 items

packages\story-lifecycle\tests\test_repair_packet.py::test_repair_packet_contains_stage_and_round PASSED [ 20%]
packages\story-lifecycle\tests\test_repair_packet.py::test_repair_packet_contains_each_finding_description PASSED [ 40%]
packages\story-lifecycle\tests\test_repair_packet.py::test_repair_packet_contains_plan_summary_and_verification PASSED [ 60%]
packages\story-lifecycle\tests\test_repair_packet.py::test_repair_packet_empty_findings_does_not_crash PASSED [ 80%]
packages\story-lifecycle\tests\test_repair_packet.py::test_repair_packet_write_to_disk PASSED [100%]

============================== 5 passed in 0.30s ==============================
```

**结论**:✅ 全绿通过。

## 发现(Findings)

无。`build_repair_packet` 行为符合任务卡契约。

## 交接 NOTE(下一窗口接什么)

- **本卡是否完成**:完成。
- **下一窗口接什么**:继续 T1.5 · Finding 生命周期(quality 飞轮)。
  被测函数在 `orchestrator/evaluation/quality.py`:
  `record_finding`(51)、`update_finding_status`(70)、`record_verification`(105)、
  `propose_learned_pattern`(261)、`approve_pattern`(291)、`activate_pattern`(296)。
  用临时 DB fixture(参考 `conftest.py` 的 `_isolated_db`),不污染真实库。
- **踩过的坑**:
  - `build_repair_packet` 写盘路径通过 `safe_story_path` 构造,测试里直接用 `tmp_path` 做 workspace 即可,
    不需要额外处理 story_key 中的特殊字符。
  - 断言用子串包含,避免锁死中文文案或 markdown 格式。
- **保留的上下文**:
  - 测试文件:`packages/story-lifecycle/tests/test_repair_packet.py`
  - 被测函数:`story_lifecycle/orchestrator/evaluation/evaluator_loop.py:29 build_repair_packet`
