# T2.5 · PTY/HITL bug 回归(最近 5 个 fix(pty))

> 任务卡:[tasks.md#T2.5](../tasks.md)
> 日期:2026-07-08 · 窗口:#1
> 状态:[已完成]

## 做了什么(What)

按任务卡要求阅读 `docs/handoff-design-hitl.md`,明确最近 PTY 注入试错链
(522d26a8 → c6dda715 → a3924b72 → 1e295d0c → 5e65535c + 3fefbd65)最终收敛到
**`claude "query"` 模式**:把 prompt 作为 CLI 初始消息传入,由 claude 自己管理 readiness,
彻底绕过 PTY 注入、readiness marker 猜测和 180s fallback。

新建 `packages/story-lifecycle/tests/test_pty_injection_regression.py`,回归当前正确行为:

1. `interactive_launch_cmd(model, prompt="X")` 返回 `["claude", "X"]`。
2. `interactive_launch_cmd(model, prompt="")` 返回 `["claude"]`(空白 claude,无多余 arg)。
3. 带 session_id/name/resume 的组合参数仍正确生成 argv。

未测试已废弃的 bracketed-paste / readiness-marker / PTY 注入逻辑。

## 代码改动(Changes)

| 文件 | 改动 | 说明 |
|---|---|---|
| `packages/story-lifecycle/tests/test_pty_injection_regression.py` | 新建 | T2.5 PTY bug 链回归测试 |
| `docs/qa-program/README.md` | 修改 | T2.5 状态更新为 `[已完成]` |

commit: _(待提交后补)_

## 验收结果(Acceptance — 必须实际跑过)

**命令**:
```bash
./.venv-monorepo-test/Scripts/python.exe -m pytest packages/story-lifecycle/tests/test_pty_injection_regression.py -v
```

**输出**:
```
============================= test session starts =============================
platform win32 -- Python 3.12.10, pytest-9.1.1, pluggy-1.6.0 -- D:\github\story-lifecycle\.venv-monorepo-test\Scripts\python.exe
cachedir: .pytest_cache
rootdir: D:\github\story-lifecycle\packages\story-lifecycle
configfile: pyproject.toml
plugins: anyio-4.14.1, asyncio-1.4.0
asyncio: mode=Mode.STRICT, debug=False, asyncio_default_default_loop_scope=None, asyncio_default_test_loop_scope=function
collecting ... collected 4 items

packages\story-lifecycle\tests\test_pty_injection_regression.py::test_interactive_launch_with_prompt_uses_query_mode PASSED [ 25%]
packages\story-lifecycle\tests\test_pty_injection_regression.py::test_interactive_launch_without_prompt_starts_blank_claude PASSED [ 50%]
packages\story-lifecycle\tests\test_pty_injection_regression.py::test_interactive_launch_with_session_id_and_prompt PASSED [ 75%]
packages\story-lifecycle\tests\test_pty_injection_regression.py::test_interactive_launch_resume_with_prompt PASSED [100%]

============================== 4 passed in 0.31s ==============================
```

**结论**:✅ 全绿通过。

## 发现(Findings)

无。当前 `interactive_launch_cmd` 行为与 handoff 文档 §11 最终方案一致。

## 交接 NOTE(下一窗口接什么)

- **本卡是否完成**:完成。
- **下一窗口接什么**:阶段二完成(T2.1-T2.5 全部完成),继续阶段三 **T3.1 · clarify MCP 阻塞 + 交互式双路径**。
  被测函数:
  - `orchestrator/mcp/clarify_server.py:76 handle_clarify_call`
  - `orchestrator/mcp/clarify_server.py:130 poll_clarify_answer`
  - `engine/prompt_sections.py build_design_dimensions_section`
  目标:MCP 路径真阻塞到人答;交互式分支文案含「在终端问人」而非「调 mcp__lifecycle__clarify」。
- **踩过的坑**:
  - docstring 里不要嵌套 triple-backticks,会报 SyntaxError。
  - `resolve_executable("claude")` 在测试环境返回的是 `claude`(或实际路径),断言时用同一函数避免硬编码。
- **保留的上下文**:
  - 测试文件:`packages/story-lifecycle/tests/test_pty_injection_regression.py`
  - 被测函数:`story_lifecycle/knowledge/adapters/claude.py:33 interactive_launch_cmd`
  - 设计文档:`docs/handoff-design-hitl.md` §11(最终方案:claude "query" 取代 PTY 注入)
