# T1.3 · no_progress 终止 — 收尾(走 A,文档修正)

> 引用:[原报告](T1.3-gate-no-progress.md)(commit `a246e5d6`) · [复核补充](T1.3-no-progress-revisit.md)(commit `eaae4ad0`)
> 日期:2026-07-08
> 状态:[已完成] · 走 A(更新 README),T1.3 关闭

## 决策与动作

owner 裁定**走 A**:更新过期文档描述,不补实现。理由见复核补充报告:max_retries 计数兜底已被 T1.1 验证有效,no_progress 是 LangGraph 时代复杂度,补回去等于回退 FC 简化决策。

**改动**:`packages/story-lifecycle/README.md` §收敛条件段:
- 删掉过期的 `no_progress`(连续无改善自动终止)和 `max_rounds`(达到上限)两行——它们是 LangGraph 时代三角色循环的收敛条件,FC 重写后已移除。
- 改为准确描述:`max_retries`(修复轮次超上限,硬闸强制 fail 等人工)。
- 加一段 ⚠️ 说明,指向 ARCHITECTURE.md §死代码已清,标注「FC 靠 max_retries 兜底,不再做 progress 检测」。

## 代码改动

| 文件 | 改动 | 说明 |
|---|---|---|
| `packages/story-lifecycle/README.md` | §收敛条件段重写 | 删 `no_progress`/`max_rounds` 过期描述,改为 `max_retries` + FC 说明 |

## 验收

无代码变更,无测试可跑。验收 = README 文档与实现一致。

- ✅ README §收敛条件不再宣称 `no_progress` 系统级检测
- ✅ 指向 ARCHITECTURE.md §死代码已清(detect_no_progress 删除记录)
- ✅ 与 T1.1 验证的 `max_retries` 硬闸行为对齐

## 结论

T1.3 走第 3 分支(文档/实现不符),经 owner 决策走 A(更新文档),现已关闭。

**整个 QA Program 第一轮 20/20 卡全部完成。** 阶段一至五全覆盖,按业务模块 ①–⑦ 体系化测试建立完毕。

## 遗留(不阻塞,转入后续可选卡)

- **死代码清理**(低优先级):`shadow_router.py:31/99/253` + `debug_packet.py:61` 的 `no_progress` 残留分支仍在读已不存在的信号,建议单开卡删除。
- **judge rework 兜底**(中等优先级):`gate.py:229-238` judge 判 rework 时返回 retry 不检查 max_retries——见 [T1.1 报告](T1.1-gate-hard-fail.md) Findings。建议开 T1.6 单独修。
