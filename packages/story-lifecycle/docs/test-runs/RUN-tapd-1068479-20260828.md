# RUN — tapd-1068479【HC】安卓和 IOS 支持配置强更最低系统版本(2026-08-28)

> RUN-5:**SOP V1.7 适配首跑**——本轮先落了 TAPD 自定义工作流映射(status_N)+暂缓旁路+中文状态名(98f2a1fe)+公司文档模板注入(verify 系统测试报告/design 技术设计文档,2bb9f226),本 run 验证适配后的全流程并产出 SOP 要求的两份文档。范本:RUN-tapd-1068343。

## 概要

- TAPD: 1144381896001068479(status_2 待开发 → 本地 lifecycle=开发,P0,due 2026-08-31)
- 需求:HC-APP 强更支持按系统版本判断——通用配置 {miniAndroid, miniIos} + 强更接口按系统版本返回最低 app 版本号(低于阈值/不可解析 → 返 1.0.0 不强更)
- 选它原因:待开发全新 story(前三跑 1068343/1068513/1068063 均已跑过)+涉及安卓端,APP UI 冒烟用例天然适用(MuMu 模拟器已接,Android 15,HappyCash 包名 com.happy.cash.fast.credit.loan.mobile.lending)
- **环境异常(本 run 关键背景)**:DeepSeek key 欠费(402)。规划 LLM 失败 → 静默 fallback `_default_planning_actions`(design/grill=true → build → verify);judge LLM 失败 → fallback approve+人确认兜底;verify 走 HcPytestVerifyProvider(不依赖 LLM)。**全流程在无 LLM 判定层下人盯推进**——本身是对降级路径的一次实测
- PRD:手写(TAPD description 4 点 → PRD 正文,/start content 传入,落 D:/hc-all/story/1144381896001068479-HC安卓和IOS支持配置/PRD.md)
- 分支:feature/ice/tapd-1068479_min_os_force_update_0828;绑定 hc-config(1)+hc-user(3)

## 适配验证点(本 run 要盯的)

| 验证点 | 结果 |
|---|---|
| 新 state_map:status_2→开发(sync 新建 story 直接 lifecycle=开发) | ✅ 同步即生效 |
| tapd_status_name 中文译名落库(待开发) | ✅ |
| 防回退:存量 story(测试态)同步 status_2/3 不回写 | ✅ 1067448/1068513/1068343 未回退 |
| design prompt 注入《技术设计文档》骨架(架构/改动点/影响/风险) | 跑时验 |
| verify prompt 注入《系统测试报告》模板(五段+用例表+≥80% 冒烟门) | 跑时验 |
| 产出 spec.md 按技术设计文档结构 | 跑时验 |
| 产出 test-report.md 按公司模板结构 | 跑时验 |

## 时间线

| 时间 | 事件 | 备注 |
|---|---|---|
| 00:28 | serve 重启(98f2a1fe+2bb9f226) | 编排线程 5s poll 正常 |
| 00:31 | POST /api/sync/tapd | created 18/updated 4;1068479 status_2→开发 ✓ 译名 ✓ |
| 00:33 | /start(project_ids=[1,3], branch, 手写 PRD) | PRD 落盘 ✓ _agent_actions fallback 生成 ✓ |
| 00:34 | /plan/confirm → 编排线程接管 | confirmed=True,lifecycle=开发 |
| 00:36 | /sessions/spawn design(claude,交互式) | session 83fa27b2;prompt_design.md 含 SOP 设计骨架 ✓ |
| 00:36-01:38 | claude 调研+设计(1h02m) | 「调研强更接口与版本比较现状」→「设计 minOS 强更方案并写 spec.md」 |
| 01:38:30 | Write 355 行被 hook 拦(>300)→ 分块写完 spec.md(354 行) | 随后 PTY 静默(claude 停在输入符等下一步指令) |
| 01:45 | supervisor stuck_detected(idle>300s) | spec.md 已在盘,agent 卡在「等输入」而非死循环 |
| 01:46 | **人工介入①**:WS 连 PTY 发 nudge「执行 story tool declare spec」 | 交互式会话等输入=合法介入点,非 PTY 注入纠偏 |
| 01:49 | declare 成功:story_doc spec v1 + design.json | artifact_declared ✓ |
| 01:49-01:51 | judge LLM 失败(DeepSeek 402)→ fallback escalate ×2 → paused | **发现死循环**:/advance → 重跑 judge → 再 escalate → 再 pause |
| 01:53 | 验证 opencode zen 网关:UA 过 CF 后 401 Insufficient balance | DeepSeek+zen 双钱包欠费,云上判定层无法恢复 |
| 01:53 | **人工介入②**:代行 judge approve——人工审阅 spec(结构完整/链路核实)后写 _completed_stages+=design + /advance | 复刻 handle_approve 效果(summary 一并落 ctx) |
| 01:53 | design→build 推进成功,/sessions/spawn build(opencode) | session ec1829f5 |
| 01:54 | **opencode zen 网关欠费**,build PTY 卡在余额页 | opencode 不可用(与 judge 同源欠费) |
| 01:56 | **人工介入③**:杀 opencode PTY + ctx._agent_actions build.adapter→claude;再 spawn 踩「marker 残留→resume 死 sid」坑(删 session_build.json 后 NEW spawn 成功) | adapter 切换 = 改 action 字段,非 spawner 特判(符合 adapter 契约) |
| 01:57-02:22 | claude build(25min):调研 journeys→补双基线→hc-config/hc-user 双 worktree 实现→declare git | 6 文件+92 行;api 模块 bump 遵约定;ddl.sql+test-plan.md 落 story 目录 |
| 02:22 | judge escalate(LLM 欠费)→ **人工介入④** approve build(审阅两仓 diff)→ advance verify | hc-config ae47725 / hc-user 8e92bb7f |
| 02:25-03:16 | claude verify(51min):探测部署→两仓单测→红绿基线回归(真实 test 环境)→写 test-report.md | 详见产出段 |
| 03:18 | verify declare test_report → judge escalate → **人工介入⑤** approve verify → 引擎 completed | docs 齐:prd/spec/git/test_plan/ddl/test_report |
| 03:20 | PUT /lifecycle/advance 首次 409「还差:代码变更」→ POST /deliverables/code/confirm → 二次 advance **✅ lifecycle 开发→测试** | advance_precheck_cmd 规范校验通过;409 语义正确拦住了未确认推进 |

## 人工介入

| # | 时间 | 方式 | 原因/效果 |
|---|---|---|---|
| ① | 01:46 | WS 连 PTY 发文字 nudge(交互式会话正等输入) | claude 写完 spec 停在提示符,提醒执行 declare → 生效 |
| ② | 01:53 | 代行 judge approve design(人工审阅 spec 后写 _completed_stages) | judge escalate 死循环(无 LLM)破局;复刻 handle_approve 的状态效果 |
| ③ | 01:56 | build adapter opencode→claude(改 _agent_actions 字段)+ 清 session marker | opencode zen 欠费;marker 残留导致 resume 死 sid,删后 NEW 成功 |
| ④ | 02:22 | 代行 judge approve build(审阅双仓 diff:hc-config 6 文件/hc-user 豁免逻辑) | 同② |
| ⑤ | 03:18 | 代行 judge approve verify(审阅 test-report.md 内容与实测证据) | 同② |

## 产出与验收

### design 阶段 ✅(人工 approve)

- `story/spec.md`(354 行)按 SOP《技术设计文档》骨架组织 ✓:
  §0 需求一句话 / §1 现状分析(逐一核实的代码链路:hc-config 存储下发/hc-user 版本比较工具/请求头已有系统版本号) / **§2 整体架构** / **§3 核心改动点** / **§4 影响范围** / §5 判定算法(伪代码)+接入点 / §6 接口契约(新增 C 端 GET /api/v1/config/min-os-version + Feign ConfigValueApi.getMinOsVersion + DTO) / §7 数据模型 / §8-10 一致性/性能/降级回滚 / §11 安全
- SOP 四要素全覆盖:整体架构✓ 核心改动点✓ 影响范围✓ 风险与应对(§10 降级/兼容/回滚)✓

### build 阶段 ✅(人工 approve)

- hc-config worktree `ae47725`:APP_MIN_OS_VERSION 通用配置 + C 端接口 + Feign 窄接口(api pom bump→1.0.10-SNAPSHOT,遵 AGENTS.md 约定)
- hc-user worktree `8e92bb7f`:needUpdateApp 豁免重载(向后兼容)+ MinOsVersionComponent + 语义化版本比较,兜底全 FALSE
- `ddl.sql` / `test-plan.md` 按 hc-all 双基线约定落 story 目录 ✓

### verify 阶段 ✅(人工 approve)——SOP《系统测试报告》模板完整落地

- `story/test-report.md` 五段结构齐全:概述/用例执行概览表/缺陷汇总/核心指标/结论 ✓
- 用例表含「APP UI 冒烟」行,纯后端 story 如实标「不适用」并说明覆盖方式 ✓
- **全部实测数据**:test 环境真实部署(build #162/#1599);红基线 2 次全过红转绿;语义化版本边界(15.10 vs 15.1 不豁免);master 基线复跑比对单测预存失败(55/1F/2E 同款);主链路回归 9/9(648s)
- **冒烟门**:合计 28/28=100% ≥80% → 「同意按计划上线」✓(SOP V1.7 要求)
- agent 自主发现并修复 hc-config-api 版本误定(1.0.9-RELEASE→1.0.10-SNAPSHOT)并验证 Nexus 发布

### lifecycle 收口 ✅

- 开发→测试:advance_precheck_cmd 规范校验通过 + code deliverable 人工 confirm + 409 gate 语义正确
- 最终状态:引擎 completed,lifecycle_state=测试(TAPD 侧状态流转留人工,不自动回写)

## 新坑与经验

| 坑/经验 | 处理 |
|---|---|
| DeepSeek 欠费(402)intake/preview 不可用 | 手写 PRD 走 /start content;规划走 fallback 默认动作(可用) |
| **judge escalate 死循环(无 LLM)**:/advance→重跑 judge→escalate→pause,永不前进 | 人工代行 approve(写 _completed_stages)+/advance。建议:#1914 类改进——LLM 调用失败的 fallback 应与「无 api_key」区分,或 escalate 后二次 advance 跳过重判 |
| Write hook 300 行上限拦 355 行 spec | agent 自行分块重写成功;hook 与 agent 协作正常 |
| 交互式 claude 写完产物后停在输入符等指令(非退出) | supervisor 判 stuck(idle>300s)但真实原因是等用户输入;WS nudge 是合法介入手段 |
| opencode zen 网关被 CF bot 规则拦(1010),带浏览器 UA 可过但余额也欠费 | 云上判定层全灭时的人工推进路径:审阅产物→_completed_stages→advance |
