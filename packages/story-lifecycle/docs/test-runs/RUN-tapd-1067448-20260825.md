# RUN — tapd-1067448 离线提额（知识注入+前后测试联调, 2026-08-25）

> 目的：真实 story 验证知识注入链路（M1/M3）+ 执行前后跑测试流程（hc-all 手工版）+ M2 走查。
> 联调 runbook：`D:/hc-all/docs/2026-08-24-joint-debug-knowledge-and-tests-runbook.md`

---

## 配置

| 字段 | 值 |
|---|---|
| Story key | `tapd-1144381896001067448` |
| 标题 | 【HC】离线提额支持审批后马上执行 |
| Profile | `minimal` |
| 模式 | 新建需求（CLI `story sync --id`，workspace=D:/hc-all） |
| 执行窗口 | ZCode 会话（API 驱动，复制提示词模式模拟） |
| 开始时间 | 2026-08-25 |
| serve 版本 | `e697f998`（M2 `15cc9b84` + M3 `e697f998` 均含） |
| 最终状态 | 已完成（4 步联调+伴生验证全通过；story 本身待真实开发） |

---

## 前置依赖就绪清单（2026-08-25 实测）

| # | 依赖 | 检查方法 | 结果 | 状态 |
|---|---|---|---|---|
| A | venv editable（serve 加载 M2/M3 新代码） | `import story_lifecycle.__file__` | 指向 `packages/story-lifecycle/src` | ✅ |
| B | miner 可导入（M2 触发前提） | venv 内 `import miner` | OK（story-miner/miner） | ✅ |
| C | LLM 配置 | config.yaml | api_key 已设 | ✅ |
| D | story.db | 文件存在 | 240MB | ✅ |
| E | 知识根有货 | `D:/hc-all/.story/knowledge/` | INDEX/playbooks/failures 全在；今日 10:20 有内容更新，INDEX 落后 1s → 读时自愈处理 | ✅ |
| F | hc-pytest cli 助手 | cli_sql.py / cli_rocketmq.py | 两脚本都在 | ✅ |
| G | 测试环境网关连通 | curl test.business-{user,order} | HTTP 404（根路径无路由，连通正常） | ✅ |
| H | codegraph | CLI + .codegraph/codegraph.db | 都在 | ✅ |
| S | serve 进程 | 8180 健康检查 | **已启动**（pid 34904 @10:32，health OK，import 实测解析到 `packages/src` = e697f998 新代码） | ✅ |
| T | hc-pytest 基线状态（A0） | — | **A0 未执行**：存量红名单无、diff 脚本无 → 本轮口径"改前基线即存量红" | ⚠️ 降级可行 |

**结论：A-H 全绿，唯一硬前置是启动 serve（S）；A0（T）缺失用基线口径兜底，不阻塞。**

---

## 联调步骤进度（2026-08-25 定稿：按用户 4 步计划走）

- [x] **Step 0** 启动 serve ✅ pid 34904，health OK，e697f998 新代码
- [x] **Step 1** 需求建 story ✅ tapd-1144381896001067448，task_type=credit-limit（详见 §Step 1 结果）
- [x] **Step 2** 覆盖判定 ✅ 直接覆盖 0 条（B 瘩后台无 journey 能力），间接 7 条（详见 §Step 2 覆盖判定）
- [ ] **Step 3** 真实试跑：3 条代表 journey 改前基线（full_lifecycle / old_user_repeat_borrow / withdraw_manual_review）
- [ ] **Step 4** 改代码前后对比：同集再跑，只看新增失败
- [ ] 伴生：plan/stream → prompt 知识段 grep → knowledge_injected 事件查询
- [ ] 收尾：时间线/发现清单填全 + 改名 RUN-tapd-1067448-20260825 + README 行更新

## Step 1 结果（2026-08-25）

- story: `tapd-1144381896001067448`【HC】离线提额支持审批后马上执行
- 创建路径：CLI `story sync --id` → `sync_tapd`（M1 打标路径，workspace=D:/hc-all）
- **task_type=credit-limit ✅**（"离线提额"正确分类，M1 验证通过）
- 需求要点：① 后台批量调额文件上传加「时效标签」（当日/次日，当日≤5000 条参数控制）② 手动调额列表页展示标签 + 停止/继续按钮 ③ 审批后每 15 分钟扫描"当日"任务发起调额（决策引擎并发≤1）
- 改名：本文件应改为 `RUN-tapd-1067448-20260825.md`（收尾时做）

## Step 2 覆盖判定（2026-08-25）

| 维度 | 结论 |
|---|---|
| 直接覆盖（提额/调额/后台审批场景） | **0 条**。25 条 journey 全部是 C 瘫 App 流（注册/授信/借款/还款/风控）；本需求是 **B 瘫后台管理功能**——conftest.yaml 网关只有 user/order/limit/config 四个 C 瘫网关，**无 91xx admin 网关**，journey 框架层面就够不到后台接口 |
| 间接覆盖（hc-limit 回归保护） | 7 条 journey 触达 hc-limit（full_lifecycle / old_user_repeat_borrow / manual_reject / payment_fail / risk_close / risk_reject / withdraw_manual_review）——调额逻辑若伤及借款链路读额度可被捕获 |
| `limit/` `user/` 子目录 | 空目录（预留未填） |
| 结论 | **coverage.md 应判 PARTIAL，未覆盖影响点=本需求的全部三个功能点**。系统性缺口：hc-pytest 无后台网关能力 → 候选改进（admin 网关接入 or 后台功能走 API 直驱 flow） |
| Step 3 基线选集 | 3 条代表：`full_lifecycle`（最广）/ `old_user_repeat_borrow`（额度使用核心，与提额最相关）/ `withdraw_manual_review`（审批人审流最接近） |

---

## 时间线

| 时间 | 事件 | 备注 |
|---|---|---|
| 08-25 10:2x | 知识根内容有更新（外部会话） | INDEX 落后 1s，自愈兜底 |
| 08-25 | 前置依赖检查 A-H 全绿 | 见上表 |

---

## Step 2.5 补测试（2026-08-25 用户纠正后补——闭环的关键缺步）

> 纠正：只"如实报告无覆盖"不构成闭环。正确闭环 = **发现缺口 → 补测试（红基线）→ 实现需求 → 部署 test → 同用例重跑变绿**。对比的意义建立在"有为本需求而写的用例"上。

- 新增 journey：`hc-pytest/journeys/limit/manual_adjust_same_day.yaml`（`limit/` 空目录的首个居民）
- 场景：注册用户 → 构造 `timeliness_tag='SAME_DAY'` 的 APPROVED 批次 → backdoor 触发 → 断言调额发起（明细离开 PENDING + `t_limit_increase_apply` 有新申请）
- **红基线实测（2026-08-25 02:5x）：`1 failed`，失败 stage=「构造当日调额批次」，INSERT 带 `timeliness_tag` 列失败（列不存在）**——锚定"需求未实现"
- 契约提议（实现时可对齐并同步改用例）：列名 `timeliness_tag`、值 `SAME_DAY/NEXT_DAY`；断言口径=「调额被发起」（成功依赖决策引擎，不在本用例承诺内）；触发暂用 `/api/back/retryBatchAdjustByBatchId`，落地后应换当日扫描的等价可触发入口（可测性要求）

## Step 3/4 结果（2026-08-25）

**改前基线**（`-k "repeat_borrow"`）：

| journey | 结果 | 备注 |
|---|---|---|
| gen_repeat_borrow_1726138 | PASSED | 全批 152s |
| old_user_repeat_borrow | PASSED | 约 75-80s/条 |

**改后重跑**（hc-limit `UserLimitServiceImpl.java` 加探针注释后同集再跑）：2 passed / 158s → **新增失败 0、恢复 0，基线稳定（两轮全绿无环境抖动）**。探针已还原。

结构性发现见 #4：journey 黑盒打远程测试环境——本地 diff 永远影响不到结果，"改后"的真正锚点是 **test 部署完成后**。

## 伴生验证（知识注入链路，2026-08-25 02:49）

- `GET /plan/stream`：规划成功（design→build→verify 三阶段，task_actions 正确）
- `build_knowledge_section(key, 'D:/hc-all', 'design')`（= prompt_renderer:298 spawn 链路同函数同参数）：注入 **1853 字符，全量层**（非降级），头部 `## 飞轮知识上下文（task_type=credit-limit）`，内容精准命中——hc-limit 关键表列出 **`t_limit_increase_apply`（提额申请表）**、`t_credit_balance_adjust`、`t_user_limits`
- **M3 事件落库**：`knowledge_injected` 1 条，payload `{"task_type": "credit-limit", "chars": 1853, "degraded": false}` ✅
- 注：prompt_<stage>.md 在 spawn 时才落盘（复制模式入口待确认，见 #6），故直接验证渲染函数

---

## 发现清单

> 跑测中发现的问题（BUGLOG 级）/ 异常现象，逐条编号。

| # | 现象 | 影响 | 状态 |
|---|---|---|---|
| 1 | 后台任务取消后 python 子进程不死：第一次 serve 启动的任务被取消，只杀了 shell 包装，detached python（34904）存活并占住 8180；第二次启动 10048 端口竞争失败退出 | 端口占用误判"serve 挂了"；stop 手册的 taskkill 路径是对的 | 已记录（run-story-serve skill 的停法已覆盖） |
| 2 | run-story-serve skill 警告"全局 python = site-packages 旧拷贝"已过时：实测全局 Python312 `import story_lifecycle` 解析到 `packages/story-lifecycle/src`（editable 同样在全局生效） | 该警告不再构成"必须用 venv"的理由（venv 仍有 web/ 新构建的优势）；skill 文档可更新 | 已记录，待更新 skill |
| 3 | journey 映射按文件名统计会把 `skip: true` 废弃用例算进可选集（full_lifecycle / withdraw_manual_review 均 deprecated skip；实际可收集 20 条） | TIA v0 索引脚本（阶段 A）必须解析 `skip` 字段 | 已记录 → 阶段 A 需求 |
| 4 | journey 黑盒打远程 test 网关：本地代码改动不影响结果；他人部署可能污染环境 | **hc-test 流程修正**：改后回归触发点 = test 部署完成后（delivery/deploy gate 后），非本地 commit 后；改前基线价值 = 贴近自己部署时刻的环境快照 | 已记录 → hc-test skill 设计修正 |
| 5 | 本需求是 B 瘩后台功能（admin 调额），conftest.yaml 仅 4 个 C 瘩网关，无 91xx admin 网关 → 直接覆盖 0 条 | coverage.md 判 PARTIAL；系统性缺口：admin 网关接入或后台 API 直驱 flow 候选改进 | 已记录 → coverage.md 结论 |
| 6 | prompt_<stage>.md 在 spawn 时才落盘；复制提示词模式（不经 spawn）下 `.story/context/<key>/` 为空、`GET /prompts` 返回空 | 复制模式的提示词来源入口待确认（UI 哪个动作产出可复制 prompt）；若确无入口是 copy-prompt 模式缺口 | 待用户澄清 |
| 7 | **选择集不可发现**：补了红基线 journey 后，没有任何机制让接手实现的 agent 知道"该跑它"——信息没物化到 agent 会经过的路径 | 已补物化链：story 目录 `test-plan.md`（红基线+回归集+命令+部署锚点铁律）+ story-context 注册 kind=test_plan（id 294，pack 已验证携带）；系统性修正=测试选择集必须在实现开始前生成（见 test-plan §2.3 修正） | 已修（手工版）；hc-test skill 落地后固化为第一步 |
| 8 | **只有红基线不够（用户二次纠正）**：需求默认档"次日"=现有 T+1 行为，无任何用例锁它 | 已补绿基线 `manual_adjust_next_day.yaml`：直发 MQ `MANUAL_BATCH_ADJUSTMENT_LIMIT_LISTEN` 驱动真实 listener 链 → **实测 PASSED**。终态双基线：next_day 绿（改后必须保持）+ same_day 红（改后必须转绿） | 已修（双基线形态） |
| 9 | backdoor `retryBatchAdjustByBatchId` 对合成批次抛异常（泛型 300；零风险对照实验：不存在批次返回原文消息、合成批次返回泛型异常文案）→ 改用 journey 直发 MQ 绕开 | 绿基线不依赖 backdoor；backdoor 自身问题遗留 test 环境日志级排查 | 已绕开；遗留日志排查 |
| 10 | hc-pytest 框架写作坑三枚（补测试实测撞上）：api trigger 无 query 参数支持（`params` 键被静默忽略，须拼进 path）；字符串断言不支持 `gte`（COUNT 断言须用结构化 `- db: ... gte: 1`）；`cleanup` 必须是 `{steps: [...]}` 字典（裸列表 AttributeError） | journey 写作规范素材，应进 A0 任务书/fixtures-playbook 或 known-limitations | 已记录 → A0 回写 |

---

## 本次沉淀

- **闭环定义修正（用户纠正）**：需求 → 覆盖检查 → **缺口则补测试（红基线）** → 实现 → 部署 test → 同用例重跑（绿）= 对比才有意义。本次已走到红基线，绿等真实现部署后验收
- **4 步联调走通**：task_type 打标 ✅ / 覆盖判定（直接 0）→ 补测试（manual_adjust_same_day 红）→ 回归基线（2 绿）→ 探针对比（0 新增）
- **伴生 M1/M3 验证通过**：credit-limit 全量层注入精准（点名 `t_limit_increase_apply` 提额申请表）+ `knowledge_injected` 事件落库
- 基线数据：journey 单条约 75-80s；20 条可收集（skip 之外）
- skill 候选：①"缺口→补红基线用例→部署后验收绿"流程规则（复现 1 次）②"改后回归锚点=部署后"（复现 1 次）
- 回写动作：hc-all runbook/test-plan 补"缺口补测试"步骤；A0 任务书补"索引读 skip"
- **流程规则固化（2026-08-25 用户两条追加）**：① 无测试场景必须**中断开发**先补双基线（已进 hc-all AGENTS.md 全栈开发约定 + runbook 硬规则）② 存量需求挖掘补测试场景 + 生产回放——新工作流落 test-plan §2.8（阶段 E）。**回放定义用户已定案（当日修正）：生产数据为基准（oracle）、测服为执行场**（生产轨迹定场景/参数/断言基准，身份映射测试账号，不在生产写入）；§2.8 初稿的 R1/R2/R3 分档作废
