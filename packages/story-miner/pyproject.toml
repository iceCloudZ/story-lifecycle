[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "story-miner"
version = "0.1.0"
description = "Mine coding-agent transcripts into LLM-ready structured knowledge"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "zhaozihao"},
]
keywords = ["ai", "agent", "transcript", "mining", "knowledge"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
# scripts/ hosts the analysis scripts imported as modules (generate_playbooks,
# failure_mode, ...); miner is the package; ../knowledge/src is the unified
# knowledge layer consumed by test_knowledge_outputs.py.
pythonpath = ["scripts", ".", "../knowledge/src"]

[tool.hatch.build.targets.wheel]
packages = ["miner"]
